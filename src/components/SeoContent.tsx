export default function SeoContent() {
  return (
    <div style={{
      position: 'absolute', width: '1px', height: '1px',
      overflow: 'hidden', clip: 'rect(0,0,0,0)',
      whiteSpace: 'nowrap', border: 0,
    }} aria-hidden="false">
      {/* SEO content will be populated with final copy */}
    </div>
  )
}
